{% extends 'base.html.twig' %}
{% block contentClass %}max-width-1400{% endblock %}

{% block pageContent %}
    <div class="row">
        <div class="col-md-10 offset-md-1">
            <div class="card">
                <div class="card-body">
                    <div class="clearfix page-header"></div>
                    {{ form_start(form) }}
                    {{ form_errors(form) }}
                    <div class="row bottom-border align-items-center pb-2">
                        <div class="col-8 col-md-6">
                           <h3 class="text-primary font-sm-19">Create Property</h3>
                        </div>
                        <div class="col-4 col-md-6 text-end">
                            <button type="submit" class="btn btn-success">Save</button>
                        </div>
                    </div>
                    {{ include('form/user_property_form.html.twig') }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}

