{% extends 'base.html.twig' %}

{% block pageContent %}
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <div class="card">
                <div class="card-body">
                    <div class="clearfix page-header"></div>
                    {{ form_start(form) }}
                    {{ form_errors(form) }}
                    <div class="row bottom-border pb-2">
                        <div class="col-md-6">
                           <h3 class="text-primary"> Create Property</h3>
                        </div>
                        <div class="col-md-6 text-end">
                            <button type="submit" class="btn btn-success">Save</button>
                        </div>
                    </div>
                    {{ include('form/user_property_form.html.twig') }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>

{% endblock %}

