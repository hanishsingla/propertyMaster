framework:
    rate_limiter:
        registration:
            policy: sliding_window
            limit: 3
            interval: '1 hour'
        reset_password:
            policy: sliding_window
            limit: 3
            interval: '1 hour'
        contact:
            policy: sliding_window
            limit: 5
            interval: '1 hour'
